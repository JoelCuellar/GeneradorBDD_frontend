/** @type {import('next').NextConfig} */
const nextConfig = {
  // Activa export estático solo si lo necesitas:
  // output: "export",
  // images: { unoptimized: true },

  reactStrictMode: true,
};

module.exports = nextConfig;
